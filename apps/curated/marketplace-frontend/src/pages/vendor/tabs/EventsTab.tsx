/**
 * Events Tab
 * Feature 011: Vendor Portal MVP
 * Phase 1: Unified Navigation
 *
 * Event management for vendor-hosted spa events.
 * Currently uses Figma EventManagement component (placeholder).
 * TODO: Evaluate scope - may be spa-facing feature or out of MVP
 */

import React from 'react';
import { EventManagement } from '../../../components/dashboard/vendor/EventManagement';

export function EventsTab() {
  // Phase 1: Use existing Figma component as placeholder
  // Decision needed: Keep in vendor portal or move to spa dashboard?
  return <EventManagement />;
}
