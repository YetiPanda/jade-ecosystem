/**
 * Inventory Tab
 * Feature 011: Vendor Portal MVP
 * Phase 1: Unified Navigation
 *
 * Inventory management with stock levels and alerts.
 * Currently uses Figma InventoryManagement component (placeholder).
 * TODO: Add to Feature 011 Phase F (future enhancement)
 */

import React from 'react';
import { InventoryManagement } from '../../../components/dashboard/vendor/InventoryManagement';

export function InventoryTab() {
  // Phase 1: Use existing Figma component as placeholder
  // Future: Add real inventory GraphQL schema
  return <InventoryManagement />;
}
